# shipskip

`shipskip` is a Bun workspace for running frontend tasks.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run dev:cli
```

To lint, typecheck, test, and build all workspace packages:

```bash
bun run check
```
