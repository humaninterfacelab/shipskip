# @shipskip/cli

CLI package for `shipskip`.

## Development

```bash
bun install
bun --filter '@shipskip/cli' dev
```

## Checks

```bash
bun --filter '@shipskip/cli' lint
bun --filter '@shipskip/cli' typecheck
bun --filter '@shipskip/cli' test
bun --filter '@shipskip/cli' build
```
